# Prototype / development runtime URLs.
# These values are intentionally committed because they contain no secrets.
# Edit them when moving the app to another Codespace or host.

FRONTEND_URL=https://obscure-invention-jvqpwpg5wx42p7vg-3000.app.github.dev

# "*" keeps prototype CORS open for localhost, Codespaces and temporary preview URLs.
# Replace with a comma-separated explicit allowlist before a public production deployment.
CORS_ALLOW_ORIGINS=*
